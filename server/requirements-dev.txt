# Development & CI dependencies (NOT linked into the distributed executable).
-r requirements.txt

pytest>=8.0          # MIT
pytest-asyncio>=0.23 # Apache-2.0
httpx>=0.27          # BSD-3-Clause (TestClient transport)
ruff>=0.4            # MIT
cryptography>=42.0   # Apache-2.0 OR BSD-3-Clause (Ed25519 appcast signing, build/release only)
pyinstaller>=6.0     # GPL-2.0-with-linking-exception (build tool only)
pip-licenses>=4.0    # MIT (licence inventory generation, spec §13.7)
